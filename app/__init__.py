"""Climber Agent Platform application package.

Root FastAPI backend package. This module initializes the package namespace
and marks it as a regular (non-namespace) package so that third-party
editable installs cannot shadow the application modules.
"""

__version__ = "0.1.0"
